#!/usr/bin/env bash
pushd ~/.dotfiles || echo "Failed to change directory" || exit
git add --intent-to-add .
sudo -v
nh os switch --build-host m_dea -a .?submodules=1 -- $@ --show-trace --accept-flake-config
#--option substituters "https://cache.nixos.org"
# --option extra-substituters https://install.determinate.systems 
# --option extra-trusted-public-keys cache.flakehub.com-3:hJuILl5sVK4iKm86JzgdXW12Y2Hwd5G07qKtHTOcDCM= 
# nh os switch -a .?submodules=1 -- $@ --show-trace
# nh os switch -a .?submodules=1 -- $@ --builders "" --show-trace
#hyprctl reload
popd || exit
